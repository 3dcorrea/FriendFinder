var express = require("express");
var path = require("path");
var route = express.Router();


route.get('/', (req, res)); {
    res.send('/ being hit');
}






















module.exports = route;